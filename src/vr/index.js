/**
 * BANANO VR
 * VR module boundary.
 *
 * Mobile VR and WebXR systems are implemented in later chapters.
 */

export function createVRModule() {
  return {
    name: "vr",
    initialize() {}
  };
}
