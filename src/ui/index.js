/**
 * BANANO VR
 * UI module boundary.
 *
 * Home, settings and diagnostics are implemented in later stages.
 */

export function createUIModule() {
  return {
    name: "ui",
    initialize() {}
  };
}
