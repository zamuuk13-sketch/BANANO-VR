/**
 * BANANO VR
 * Interaction module boundary.
 *
 * Gestures, objects and physics are implemented in later chapters.
 */

export function createInteractionModule() {
  return {
    name: "interaction",
    initialize() {}
  };
}
