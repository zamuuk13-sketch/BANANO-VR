/**
 * BANANO VR
 * Rendering module boundary.
 *
 * WebGL, scene, camera and renderer systems are implemented later.
 */

export function createRenderingModule() {
  return {
    name: "rendering",
    initialize() {}
  };
}
